jQuery(document).ready(function ($) {
  // wp.customize.panel('wpt_product_tabs_customize', function (panel) {
  //   console.log(panel)
  // })

  // var sections = wp.customize.panel('wpt_product_tabs_customize').sections()
  // console.log(sections)

  // var sections = wp.customize.panel('wpt_product_tabs_customize').sections()
  // console.log(sections)

  // wp.customize( 'wpt_product_tabs_params[wpt_product_tabs]', function( value ) {
  //   value.bind( function( newval ) {
  //     let list_tab = JSON.parse(newval || '[]') || []

  //     console.log(list_tab)
  //     $('body').css('background-color', '#fff' );
  //     wp.customize.previewer.bind( 'loading-initiated', function( message ) {
  //       console.info( 'Preview is loaded' );
  //    } );
  //   } );
  // } );
})